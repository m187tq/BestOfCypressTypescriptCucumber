describe('top Navigation Tests', () => {
  it('should navigate to home page', () => {})

  it('should populate different current domination when hovered over', () => {})

  it('should populate cart and checkout button when hovered over', () => {})

  it('should navigate to login page on click', () => {})
  it('should navigate to login page on special page', () => {})
  it('should populate dropdown login or check your order', () => {})

  it('should navigate to checkout cart page on click', () => {})

  it('should navigate to shopping cart page on click', () => {})

  it('should navigate to product id page on click search button', () => {})
})
